# Python
.venv/
__pycache__/
*.pyc

# CloudCompare's own per-plugin virtual environment (auto-created in the
# plugin folder when you say yes to its "create a virtual environment?"
# prompt — named after the plugin, not .venv)
functional_review/

# PyCharm
.idea/

# Data / large artifacts (keep these on the shared drive, not in git)
data/
*.laz
*.las
*.ply
*.h5
reviews/
review_clouds/
functional_labels_reviewed/

# Local-only batch-building notes and tools (not part of the shared reviewer
# setup — cc_functional_review.py is self-contained and doesn't need these;
# they only matter to whoever builds review clouds from Tao's pipeline .h5)
BUILDING_REVIEW_CLOUDS.md
functional.py
build_review_cloud.py
review_admin.py
export_reviewed.py
review_editor.py
