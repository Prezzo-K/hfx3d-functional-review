# Safe to auto-install into CloudCompare's plugin venv:
numpy>=1.24
h5py>=3.8

# NOTE: the Qt binding is intentionally NOT listed here. It must match the Qt
# version CloudCompare itself was built with, and installing the wrong one
# CRASHES the whole app (a Qt5 binding in a Qt6 build, or vice-versa). Install
# it by hand once, matching your CloudCompare:
#   CloudCompare 2.14 (Qt6):  pip install "PyQt6==6.8.1" "PyQt6-Qt6==6.8.2"
#   CloudCompare 2.13 (Qt5):  pip install "PyQt5>=5.15"
# (For 2.14, pin PyQt6-Qt6 to your CC's Qt6 minor version — check it via
#  Qt6Core.dll's file version, e.g. 6.8.x — see the README.)
